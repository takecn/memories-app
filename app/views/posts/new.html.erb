<% if @post.errors.present? %>
  <% @post.errors.full_messages.each do |error_message| %>
    <ul>
      <li><%= error_message %></li>
    </ul>
  <% end %>
<% end %>

<h1>新規投稿</h1>

<%= form_with model: @post do |f| %>
  <ul class="nav justify-content-center">
    <li><%= link_to "ホームに戻る", posts_path, class: "btn btn-primary" %></li>
    <li><%= f.submit "投稿する", class: "btn btn-primary" %></li>
  </ul>
  <div class="form-group">
    <%= f.label :comment, "コメント" %>
    <%= f.text_field :comment, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :memorized_on, "思い出の日付" %>
    <%= f.date_field :memorized_on, class: "form-control" %>
  </div>
  <div class="form-group">
    <%# <%= f.label :location, "思い出のロケーション" %> %>
    <%# <%= f.text_field :location, default: Date.today, class: "form-control" %> %>
  </div>
  <div class="form-group">
    <%# <%= f.label :tags, "タグ（複数タグ付けは，単語間にスペースを入れる）" %> %>
    <%# <%= f.text_field :tags, class: "form-control" %> %>
  </div>
  <div class="form-group">
    <%# <%= f.label :disclosure_range, "公開範囲" %> %>
    <%# <%= f.collection_select :disclosure_range, @posts, :group_name, :group_name, {selected: "非公開"}, class: "form-control" %> %>
  </div>
  <div class="form-group">
    <%# <%= f.label :post_images, "思い出の画像" %> %>
    <%# <%= f.file_field :post_images, class: "form-control" %> %>
  </div>
  <div class="form-group">
    <%= f.label :description, "詳細・補足" %>
    <%= f.text_area :description, class: "form-control" %>
  </div>
<% end %>
